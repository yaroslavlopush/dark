import { StyledContainer, StyledSpinner } from './Loading.styled';

export default function Loading() {

    return (
        <StyledContainer>
            <StyledSpinner />
        </StyledContainer>
    )
}
